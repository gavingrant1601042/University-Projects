/// @description cleanup those ds_lists
//cleanup is the last event called during any events where the object would no longer be used
//cleanup is called for example when game ends or when object is destroyed or when the room moves to another room
scr_cleanup()
